#include <iostream>
using namespace std;

int main() {
  int i,n,min,max,a[20];
  cin>>n;
  for(i=0;i<n;i++){
    cin>>a[i];
  }
  min=max=a[0];
  for(i=0;i<n;i++){
    if(min>a[i])
      min=a[i];
    if(max<a[i])
      max=a[i];
  }
  cout<<"min="<<min;
  cout<<"max="<<max;
}
